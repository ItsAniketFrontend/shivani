Images used by the invitation (all present):

  hero-bg.png    -> Full-screen hero background (Rajasthani mandap at golden hour)
  couple.png     -> Couple illustration, shown in the invite section
  divider.png    -> Gold ornamental divider, repeated between sections
  umbrella.png   -> Parasol, framed as a round medallion above the schedule

To swap any image, replace the file keeping the SAME filename.
Decorative images auto-hide if a file is missing.
